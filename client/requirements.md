## Packages
framer-motion | Animations and page transitions
date-fns | Date formatting

## Notes
- Images are converted to Base64 on the client before being sent to the AI endpoints and creation endpoints.
- No local auth is implemented per MVP requirements.
- Uses Shadcn UI components which are assumed to be in the project structure.
